diff --git a/mbedtls/files/mbedtls_config.h b/mbedtls/files/mbedtls_config.h
--- a/mbedtls/files/mbedtls_config.h
+++ b/mbedtls/files/mbedtls_config.h
@@ -1206,7 +1206,8 @@
  *
  * Uncomment this macro to disable the built-in platform entropy functions.
  */
-#define MBEDTLS_NO_PLATFORM_ENTROPY
+/* KOS 2.3 provides /dev/urandom; retain mbedTLS's POSIX entropy poller. */
+//#define MBEDTLS_NO_PLATFORM_ENTROPY
 
 /**
  * \def MBEDTLS_ENTROPY_FORCE_SHA256
diff --git a/stb_image/files/KOSMakefile.mk b/stb_image/files/KOSMakefile.mk
--- a/stb_image/files/KOSMakefile.mk
+++ b/stb_image/files/KOSMakefile.mk
@@ -3,3 +3,7 @@ OBJS = stb_image.o
 KOS_CFLAGS += -Iinclude
 
 include ${KOS_PORTS}/scripts/lib.mk
+
+# stb_image is C-compatible; compile it as C on toolchains built without G++.
+stb_image.o: stb_image.cpp
+	$(KOS_CC) -x c $(KOS_CFLAGS) -c $< -o $@
